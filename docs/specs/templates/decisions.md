# Decision Log: <slug>

> Mid-flight decisions made during the build. Append-only. Each entry is a lightweight ADR — capture the choice, why, and how to reverse it.

## YYYY-MM-DD — <Decision Title>

**Context:** What forced a decision (blocker, ambiguity, new constraint discovered mid-build).

**Decision:** What we chose.

**Alternatives:** What we rejected and why.

**Reversibility:** How hard to undo; what would trigger revisiting this.

**Deviations from spec:** If this departs from `spec.md`, note it explicitly (e.g., an open review gate the user accepted, or an accepted security risk with compensating controls — record it here, never fabricate a PASS).

---
